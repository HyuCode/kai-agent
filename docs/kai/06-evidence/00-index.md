# 検証記録 — 索引

この階層には、共有すべきリハーサル結果、実機評価の要約など、特定時点の事実を保存します。ここにある記録は要件・設計の正典ではありません。

配信レビューの生メモは、映像やローカル環境に依存する作業記録のため、
`.gitignore` 対象の `docs/kai/stream-review/` に保存します。共有すべき結論が得られたら、
番号付きの要約文書をこの階層へ追加し、要件・設計・運用手順にも決定を反映します。
